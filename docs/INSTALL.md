# Installing LoreLake

The installer skills (`/llake-lady`, `/llake-doctor`, `/llake-bootstrap`) are
scaffolded as `spec.md` files but not yet generated. Installation
instructions will be added here once the skills are generated via
`/skill-creator`.

For now, see `docs/specs/2026-04-18-lorelake-plugin-design.md` for the full
architecture.
