# Config

The LoreLake configuration system — the full config.json schema and the layered defaults contract.

| Page | Description |
|---|---|
| [[config-schema]] | Complete reference for all config.json keys, defaults, types, and effects |
| [[config-layering]] | How user config.json and config.default.json are merged — the dot-key lookup contract |
