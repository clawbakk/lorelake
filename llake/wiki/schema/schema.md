# Schema

The schema/ directory is the immutable spec governing all wiki writers. These pages document what the spec says and why it matters.

| Page | Description |
|---|---|
| [[schema-overview]] | The schema/ directory and how it governs all wiki writers — immutable spec, not code |
| [[page-format]] | Complete page format spec: frontmatter, required sections, naming, categories, wikilinks |
| [[content-standards]] | Standards 1, 2, 3 — new-employee test, conversation fidelity, no sensitive data |
